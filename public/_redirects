# Netlify redirects file for Single Page Application (SPA)
# This ensures that all routes are handled by React Router

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Optional: Redirect HTTP to HTTPS (uncomment if needed)
# http://yourdomain.com/*    https://yourdomain.com/:splat    301!

# Optional: API redirects (if you have a backend API)
# /api/*  https://your-backend-api.com/api/:splat  200